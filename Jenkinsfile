pipeline {
    agent any
    stages {
        stage('Clone') {
            steps {
                git branch: 'main', url: 'https://github.com/hoangnguyen208/aws-code-deploy.git'
            }
        }
    }
}